# Workflow C: Life Event Trigger

## Phase 1 Contract View

- Input enters through `TaskSpec` with intent `life_event_trigger`.
- Evidence may come from calendar events, salary changes, family changes, policy updates, or uploaded documents.
- Output will eventually update memory and state, then create downstream finance tasks and checkpoints.
- Verification will ensure event evidence coverage and safe task generation rules.
